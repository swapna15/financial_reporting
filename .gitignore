# Sensitive data — never commit
.env
*.xlsx
*.xls
*.csv
audit_logs/

# Docker — keep compose + Dockerfile but not runtime artifacts
.dockerignore

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
SAP_Guardrails_Documentation.docx
